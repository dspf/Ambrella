@* @model List<University>

<div class="container">

    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="text-primary">
                University List
            </h2>
        </div>

        <div class="col-6 text-end">
            <a asp-controller="Institutions" asp-action="Create" class="btn btn-outline-primary">
                <i class="bi bi-plus-square"></i>Institution Details
            </a>
        </div>

    </div>

    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Description
                </th>
                <th>
                    Province
                </th>
               
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>

                    <td>@obj.Name</td>
                    <td>@obj.Description</td>
                    <td>@obj.Province</td>
                </tr>
            }
        </tbody>
    </table>
    <div class="col-6 text-md-center">
        <a asp-controller="Building" asp-action="Search" class="btn btn-outline-secondary">
            Search
        </a>
    </div>
    <div>
        <a asp-controller="Inspector" asp-action="Assign" class="btn btn-primary">Assign Inspector</a>
    </div>
</div>
<style>
    .thumbnail-image {
        max-width: 100px;
        max-height: 60px;
    }
</style>
 *@